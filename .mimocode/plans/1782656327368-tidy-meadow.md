# Plan: Majlis Role — Reports-Only Access

## Goal
Add a new `majlis` role (username: `majlis`, password: `admin`) that only sees the **Reports** section. No access to Attendance, Timetable, or any other tab.

## Files to Modify

### 1. `backend/server.js` — Login Route (line ~577)

**Add a hardcoded Majlis login check** before the System Admin check (line 595). This avoids touching the Python backend or DB.

```
if (username === 'majlis' && password === 'admin') {
    const majlisUser = {
        id: "majlis-user",
        name: "Majlis",
        username: "majlis",
        role: "Majlis",
        sessionId: require('crypto').randomBytes(16).toString('hex')
    };
    // sign JWT, log activity event, return success + token
}
```

**Add `'Majlis'` to `getUserRoleLabel`** (line 147) so it returns `'Majlis'` instead of defaulting to `'Subject Teacher'`.

### 2. `frontend/src/app/page.js` — Tab Restrictions

**a) Force `reports` tab on mount** (~line 383):
After `activeTab` state, add effect: if `user?.role === 'Majlis'`, force `setActiveTab("reports")`.

**b) Guard `switchTab`** (~line 500):
If `user?.role === 'Majlis'`, only allow `tab === 'reports'` — ignore all other tab switches.

**c) Guard URL-based tab redirect** (~line 514):
In the `useEffect` that reads `searchParams`, if `user?.role === 'Majlis'`, always force `setActiveTab("reports")`.

**d) Bottom Nav — show only Reports tab** (~line 4993):
Before building the `tabs` array, check `user?.role === 'Majlis'`. If true, set `tabs` to only the reports entry (skip Attendance, Timetable, and all role-based additions).

**e) Hide report landing cards that show Admin features** (e.g. settings link, teacher list):
The reports landing page already gates admin-only cards with `user?.role === "admin"`, so Majlis won't see those. No change needed.

## Verification

1. Login with `majlis` / `admin` → should land directly on Reports tab, bottom nav shows only Reports icon
2. Try navigating to `/?tab=attendance` via URL → should redirect back to reports
3. Login with normal teacher credentials → should work as before (Attendance, Timetable, Reports tabs)
4. Login with admin credentials → should work as before
